# Hardhat workspace — isolated from the parent frontend repo
node_modules/
.env
cache/
artifacts/
typechain-types/
coverage/
coverage.json

# Hardhat Ignition / deployment artifacts (keep your own if you adopt Ignition)
ignition/deployments/

# Slither / Python venv (Slither is installed locally, kept out of package.json)
.venv/
crytic-export/

# Editor / OS
.DS_Store

# Local fork-deploy artifact (addresses only; regenerated by scripts/dev)
scripts/dev/.deployed.json

# Shieldify's delivered report (proprietary; kept local only)
Bankroll-Vault-Security-Review.pdf
*-Security-Review.pdf
